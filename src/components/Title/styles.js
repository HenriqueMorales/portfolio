import styled from "styled-components";

const StyledTitle = styled.h1`
  color: var(--blue);
  font-size: 4.6rem;
  padding-top: 4rem;
  text-align: center;

  p {
    color: #fff;
    font-size: 3.2rem;
    padding-top: 0.8rem;
  }
`;

export default StyledTitle;
